schema_version: 1

context:
  python_min: "3.11"
  version: "0.3.0"

package:
  name: signurlarity
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/signurlarity/signurlarity-${{ version }}.tar.gz
  sha256: 4f324370f7de4171591edb7a2a84f983b143930ea952f5ea076e49d28fa9153d

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >=${{ python_min }}
    - httpx >=0.24.0
    - orjson
    - cryptography >=41.0.0

tests:
  - python:
      imports:
        - signurlarity
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Lightweight library to presign URLs compatible with boto3
  license: GPL-3.0-only
  license_file: LICENSE
  homepage: https://github.com/diracx/signurlarity

extra:
  recipe-maintainers:
    - chaen
    - chrisburr
